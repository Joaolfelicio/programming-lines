export interface ISearchablePostDto {
    slug: string;
    title: string;
    description: string;
    image: string;
    publishDate: Date;
}

export interface ISearchPost {
    slug: string;
    title: string;
    description: string;
    image: string;
}